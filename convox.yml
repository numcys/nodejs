environment:
  - PORT=3000
services:
  web:
    domain: ${HOST}
    build: .
    port: 3000
balancers:
  ingress:
    annotations:
    - test.annotation.org/value=foobar
    service: web
    ports:
      3000: 1000
      3001:
        protocol: UDP
        port: 5000
