environment:
  - PORT=3000
services:
  web:
    domain: ${HOST}
    build: .
    port: 3000
balancers:
  ingress:
    annotations:
    - test.annotation.org/value=foobar
    service: web
    ports:
      8080: 3000
